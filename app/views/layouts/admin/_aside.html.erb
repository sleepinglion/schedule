<aside id="main-menu" class="col-12 col-md-2 col-xxl-1">
  <h1 class="col-12"><%= link_to t(:service_name), admin_reservations_path %></h1>
  <nav>
    <ul class="nav">
      <li class="nav-item
            <% if ['admin/reservations'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to raw('<i class="material-icons">schedule</i>'+t(:reservation, scope: [:activerecord, :models])),admin_reservations_path,:class => "nav-link"  %>
      </li>
      <li class="nav-item
            <% if ['admin/orders'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to raw('<i class="material-icons">shopping_cart</i>'+t(:order, scope: [:activerecord, :models])),admin_orders_path,:class => "nav-link"  %>
      </li>
      <li class="nav-item
            <% if ['admin/accounts'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to raw('<i class="material-icons">attach_money</i>'+t(:account, scope: [:activerecord, :models])),admin_accounts_path,:class => "nav-link"  %>
      </li>
      <li class="nav-item
            <% if ['admin/users','admin/operators'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to raw('<i class="material-icons">group</i>'+t(:user, scope: [:activerecord, :models])+','+t(:operator, scope: [:activerecord, :models])),admin_users_path,:class => "nav-link"  %>
      </li>
      <li class="nav-item
            <% if ['admin/branches','admin/receipts','admin/addresses','admin/address_categories'].include?(params[:controller]) %> active
            <% end %>">
        <%= link_to raw('<i class="material-icons">settings</i>'+t(:setting)),admin_branches_path,:class => "nav-link"  %>
      </li>
    </ul>
  </nav>
</aside>