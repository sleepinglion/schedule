<%= form_with(model: [:admin,user], local: true, :html=>{:id=>"user_form"}) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this request from being saved:</h2>
      <ul>
        <% user.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :address_id %>
    <%= form.select :address_id,options_from_collection_for_select(Address.where(:enable=>true).all, 'id', 'title',user.address_id),{},{:class=>"form-control"} %>
  </div>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, :class => "form-control", :required => "required" %>
  </div>
  <div class="form-group">
    <%= form.label :phone %>
    <%= form.text_field :phone, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= form.submit :class=>"btn btn-primary btn-lg btn-block" %>
  </div>
<% end %>
